// commonStyles.ts

import { StyleSheet } from "react-native";

export const commonTextShadow = {
  textShadowColor: "rgba(0, 0, 0, 0.75)",
  textShadowOffset: { width: 1, height: 1 },
  textShadowRadius: 3,
};

// You can add other common styles here later if needed
/*
export const commonColors = {
  primary: '#FFD700',
  secondary: 'white',
  darkOverlay: 'rgba(0,0,0,0.6)',
};

export const commonContainerStyles = StyleSheet.create({
  fullScreenContent: {
    flex: 1,
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'transparent',
  },
  overlay: {
    padding: 20,
    borderRadius: 10,
    alignItems: "center",
  },
});
*/
